#include <stdio.h>
int main(){
    printf ("nhap a b c d\n");
    int a;
    long long b;
    float c;
    double d;
    scanf ("%d %lld %f %lf", &a, &b, &c, &d);
    printf("cac so da nhap\n %d %lld %f %lf", a,b,c,d);
    return 0;
}
